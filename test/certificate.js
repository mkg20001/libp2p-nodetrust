'use strict'

/* eslint-env mocha */

describe.skip('certificate', () => { // TODO: we can't run these because the dns server needs to be exposed, find a way to expose it
  it('should aquire a localhost cert from letsencrypt') // localhost because our ip gets detected as localhost because the server is on localhost
})
